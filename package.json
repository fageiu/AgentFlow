{
  "name": "agentflow-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise workflow AI agent sandbox and evaluation platform.",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "dev:web": "pnpm --filter @agentflow/web dev",
    "dev:server": "pnpm --filter @agentflow/server dev",
    "test": "pnpm --recursive --if-present test",
    "build": "pnpm --recursive build",
    "typecheck": "pnpm --recursive typecheck",
    "eval:gate": "pnpm --filter @agentflow/server eval -- --label ci-mock-gate --mock --output .agentflow-artifacts/mock-evaluation.md"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
