{
  "name": "@agentflow/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/agent/executor.test.ts src/knowledge/ragClient.test.ts src/llm/config.test.ts",
    "eval": "tsx src/eval/evaluationCli.ts",
    "eval:compare": "tsx src/eval/evaluationCompareCli.ts"
  },
  "dependencies": {
    "@agentflow/shared": "workspace:*",
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.16.5"
  }
}
